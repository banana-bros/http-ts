import { expect } from 'chai';
import { HTTPMethod } from '../../src/enum/HTTPMethod';

describe('SecureWebSocketServer', () => {
    beforeEach(() => {
    });

    it('should be created', () => {
        expect(HTTPMethod).to.be.ok;
    });
});
